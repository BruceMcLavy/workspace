# HQV

Dev Container + Docker Compose skeleton

Stack:

* PostgreSQL 16
* .NET 8 API
* React (Vite) � apps/web

## Quick start

1. Ensure Docker Desktop (WSL 2 backend) is running.
2. Run:
   docker compose up -d
3. Open this folder in VS Code ? **Reopen in Container**.

Containers:
- hqv-db
- hqv-api
- hqv-web
